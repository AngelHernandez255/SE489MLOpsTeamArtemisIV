version: '3.8'

services:
  SE489MLOpsTeamArtemisIV:
    build:
      context: .
      dockerfile: dockerfiles/Dockerfile
    container_name: SE489MLOpsTeamArtemisIV_app
    volumes:
      - ./data:/app/data
      - ./models:/app/models
    environment:
      - PYTHONUNBUFFERED=1
    ports:
      - "8000:8000"
